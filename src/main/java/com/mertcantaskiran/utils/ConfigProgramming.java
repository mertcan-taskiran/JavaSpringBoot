package com.mertcantaskiran.utils;

public class ConfigProgramming {
    // DevTools => Live Reload
    // file > settings > build > compiler > build project automatically (OK)
    // file > settings > advanced >  allow auto make to start even (OK)
}
